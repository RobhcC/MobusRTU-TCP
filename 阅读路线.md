# ModbusRTU_TCP 项目阅读路线

> 基于 C# .NET Framework 4.7.2 + Windows Forms 的 Modbus RTU/TCP 温湿度采集上位机。
> 架构：三层架构（Model → DAL → BLL → UI），SQLite 持久化，指数退避重连，支持批量读写保持寄存器。

---

## 目录

1. [项目概览与架构](#1-项目概览与架构)
2. [阅读顺序（三段式）](#2-阅读顺序三段式)
3. [数据流全景](#3-数据流全景)
4. [关键设计要点](#4-关键设计要点)
5. [文件职责速查表](#5-文件职责速查表)
6. [常见问题与调试入口](#6-常见问题与调试入口)

---

## 1. 项目概览与架构

```
ModbusRTU_TCP
├── Program.cs              # 入口点，启动 Form1
├── Form1.cs                # UI 层：窗体、事件绑定、用户交互
├── Form1.Designer.cs       # 设计器：控件布局定义
├── App.config              # 配置中心：所有可调参数
│
├── BLL/
│   └── ModbusBLL.cs        # 业务逻辑层：通信、协议、重连、数据更新
│
├── DAL/
│   └── DataExportDAL.cs    # 数据访问层：SQLite 持久化、文件导出
│
├── Model/
│   └── DataRecord.cs       # 实体层：温湿度数据记录模型
│
└── Properties/             # 程序集信息、资源、设置
```

### 分层依赖关系

```
  Form1.cs (UI)
     │  订阅事件、调用方法
     ▼
  ModbusBLL.cs (BLL)        ←── 核心：通信协议 + 业务逻辑
     │  调用 CRUD、导出
     ▼
  DataExportDAL.cs (DAL)    ←── SQLite 存取 + TXT/CSV 导出
     │
     ▼
  DataRecord.cs (Model)     ←── 数据实体，贯穿三层
```

---

## 2. 阅读顺序（三段式）

### 🟢 第一段：从数据模型和入口入手（5 分钟）

**目标**：了解项目"做什么"——采集什么数据、怎么启动。

| 顺序 | 文件 | 阅读重点 |
|------|------|----------|
| 1 | [Model/DataRecord.cs](Model/DataRecord.cs) | 整个系统的数据载体，只有 `Id / CollectTime / Temperature / Humidity / Status` 五个字段。 |
| 2 | [Program.cs](Program.cs) | 标准 WinForms 入口，启动 `Form1`。 |
| 3 | [App.config](App.config) | **所有可调参数一览**。阅读注释就能了解系统有哪些配置项：数据库路径、超时、阈值、定时器间隔等。 |

> **一句话总结**：本系统采集温湿度传感器数据，存入 SQLite，可导出为 TXT/CSV，支持实时监控和阈值预警。

---

### 🟡 第二段：核心通信逻辑（BLL，15 分钟）

**目标**：理解"怎么通信"——Modbus 协议实现、数据收发、异常处理、重连机制。

**打开 [BLL/ModbusBLL.cs](BLL/ModbusBLL.cs)，按以下区域块阅读：**

#### 2.1 枚举与异常类（行 12-43）

- `DeviceStatus`：Online / Offline，设备状态
- `ModbusExceptionType`：区分业务异常（Modbus 协议异常码）与通信异常（超时/断连）
- `ModbusExceptionInfo`：封装异常类型、异常码、描述文本

#### 2.2 事件定义（行 103-127）

```
OnLogAdded          → 日志输出到 UI
OnDataUpdated       → 温湿度数据更新到表格
OnDataReceived      → 成功接收数据
OnBatchDataReceived → 批量读取结果
OnWriteCompleted    → 批量写入确认
OnModbusException   → 异常分类通知
OnReconnectAttempt  → 重连进度通知
```

> 事件是 BLL 向上层（UI）通信的唯一通道，**理解这些事件就理解了 UI 能做什么**。

#### 2.3 串口与 TCP 连接（行 238-416）

- **串口**：`InitSerialPort()` → `OpenSerialPort()`（常规串口操作）
- **TCP**：`ConnectTcpAsync()` 使用 `SocketAsyncEventArgs` 异步连接，配合 `TaskCompletionSource` 转为 Task 可等待。连接失败时调用 `GetSocketErrorMessage()` 输出中文友好提示。

#### 2.4 Modbus 报文构建（行 418-522）

- `BuildBatchReadCommand(0x03)`：读保持寄存器命令，6 字节 PDU + 2 字节 CRC16
- `BuildBatchWriteCommand(0x10)`：写多个寄存器命令，变长结构
- `CalculateCRC16()`：标准 Modbus CRC16 算法，查表移位实现
- `ValidateCRC()`：RTU 模式校验接收数据完整性

**数据格式图解**（读保持寄存器）：

```
RTU 模式发送帧：
┌──────┬──────┬──────────┬──────────┬──────────┬──────────┬──────┬──────┐
│ 从站  │ 0x03 │ 起始地址H │ 起始地址L │ 寄存器数H │ 寄存器数L │ CRCL │ CRCH │
│ 1 byte │ 1    │ 1        │ 1        │ 1        │ 1        │ 1    │ 1    │
└──────┴──────┴──────────┴──────────┴──────────┴──────────┴──────┴──────┘

TCP 模式发送帧（加 7 字节 MBAP 头）：
┌─────────────────────────────────┬────────────┐
│ MBAP 头 (7 bytes)               │ PDU (同上) │
│ 事务ID+协议ID+长度+单元ID       │ 少从站+CRC │
└─────────────────────────────────┴────────────┘
```

#### 2.5 数据接收与解析（行 575-814）

- `ProcessReceiveDataAsync()` → 分发到串口 / TCP 处理流程
- `ParseModbusResponse()` → 检查异常码 → 按功能码分发
- `ParseReadResponse()` → 解析温度（寄存器0/10）、湿度（寄存器1/10），触发预警判断
- `ParseBatchWriteResponse()` → 确认写入地址和数量

**温度解析逻辑**（行 738-767）：
```
寄存器0 原始值（如 0x00C8 = 200）→ (short)200 / 10.0 = 20.0°C
> 35°C → 高温预警（红色）
< 10°C → 低温预警（蓝色）
其他 → 正常（绿色）
```

#### 2.6 数据持久化入口（行 843-855）

`AddHistoryRecord()` 每收到一条有效数据就：
1. 创建 `DataRecord` 实体
2. 加入内存列表 `historyRecords`
3. 调用 `dataExportDAL.SaveRecordToSqlite()` 写入 SQLite
4. 超过最大记录数时淘汰最早记录

#### 2.7 指数退避重连（行 964-1043）

- `GetExponentialBackoffDelay(n)` = `min(1000 × 2ⁿ, 30000)`
- `StartExponentialReconnect(action)` 用 `System.Threading.Timer` 执行重连逻辑
- `StopExponentialReconnect()` 停止并清理资源
- 业务异常（Modbus 异常码）**不触发重连**，通信异常（超时/断连）**触发重连**

#### 2.8 资源释放（行 1122-1176）

`Dispose()` 模式：停止重连定时器 → 关闭串口/TCP → 释放 DAL → 解绑事件 → 清空历史记录。析构函数兜底。

---

### 🔵 第三段：UI 层与数据访问层（15 分钟）

**目标**：理解"用户怎么操作"和"数据怎么存"。

#### 3.1 UI 层 [Form1.cs](Form1.cs)

| 方法区域 | 行号 | 功能 |
|----------|------|------|
| 事件绑定 | 39-49 | 将 BLL 事件挂接到 UI 处理方法 |
| 表格初始化 | 248-278 | 显示寄存器地址/名称/值/状态 |
| 数据库视图 | 280-306 | 切换到 SQLite 记录浏览模式 |
| 自动采集 | 228-234 | 每 3 秒自动发送读取指令 |
| 打开连接 | 408-483 | 串口/TCP 连接入口，异步 TCP |
| 发送指令 | 564-607 | 构建命令 → 发送 → 超时计时 |
| 重连处理 | 637-681 | 用户关闭判断 → 指数退避重连 |
| 批量操作 | 981-1085 | 批量读取/写入寄存器 |
| 数据导出 | 710-781 | TXT/CSV 导出 |
| 数据库 CRUD | 784-929 | 增删改查对话框 |
| 控件事件 | 236-246 | 串口/波特率选择变更 |

**UI 布局结构**（来自 [Form1.Designer.cs](Form1.Designer.cs)）：
```
┌─────────────────────────────────────────────────────────┐
│ 协议选择 [RTU ▼]  串口参数 [COM1] [9600]  TCP [IP][端口]│
│ [打开连接] [关闭连接] [发送数据]                         │
├──────────────────────────┬──────────────────────────────┤
│ 寄存器表格               │ 批量操作面板                  │
│ 地址 │ 名称 │ 值 │ 状态 │ 从站地址 │ 起始地址 │ 数量   │
│ 0x00 │ 温度 │ 25.0 │ 正常│ [批量读取] [批量写入]       │
│ 0x01 │ 湿度 │ 60.0 │ 正常│ 写入值: ____________       │
│ 0x02 │ 运行 │ Run  │     │                              │
├──────────────────────────┴──────────────────────────────┤
│ 数据库操作 [查询] [新增] [修改] [删除] [清空]           │
│ 导出操作 [导出TXT] [导出CSV]                            │
├─────────────────────────────────────────────────────────┤
│ 操作日志列表                                             │
│ 2026/08/20:10:00:00 打开串口成功                        │
└─────────────────────────────────────────────────────────┘
```

#### 3.2 数据访问层 [DAL/DataExportDAL.cs](DAL/DataExportDAL.cs)

| 方法 | 功能 |
|------|------|
| `SaveRecordToSqlite()` | 插入记录，回写 `record.Id` |
| `GetAllRecordsFromSqlite()` | 查询全部，按时间倒序 |
| `GetRecordById()` | 按 ID 查询 |
| `QueryRecordsByTimeRange()` | 时间范围查询 |
| `UpdateRecord()` | 更新记录 |
| `DeleteRecordById()` | 按 ID 删除 |
| `DeleteRecordsByTimeRange()` | 时间范围删除 |
| `ClearAllRecords()` | 清空表 |
| `ExportToTxtAsync()` | 导出 TXT（带格式标题） |
| `ExportToCsvAsync()` | 导出 CSV（逗号分隔） |

**数据库表结构**：
```sql
CREATE TABLE ModbusDataRecords (
    Id          INTEGER PRIMARY KEY AUTOINCREMENT,
    CollectTime TEXT    NOT NULL,  -- yyyy-MM-dd HH:mm:ss
    Temperature REAL    NOT NULL,  -- 摄氏度
    Humidity    REAL    NOT NULL,  -- 百分比
    Status      TEXT    NOT NULL   -- 正常/高温预警/低温预警
);
```

**线程安全**：所有 SQLite 操作通过 `_dbLock` 互斥锁保护，连接池最大 10 个连接。

---

## 3. 数据流全景

### 3.1 数据采集流程（正向）

```
Timer 触发 (3s)
    │
    ▼
btnSendData_Click()
    │  BuildBatchReadCommand(0, 2)
    ▼
SendByProtocolAsync() ──→ 串口/TCP 发送报文
    │
    ▼
timerReceive 轮询接收 ←── 设备响应
    │
    ▼
ProcessReceiveDataAsync()
    │
    ▼
ParseModbusResponse()
    ├── 异常码? → OnModbusException (业务异常跳过重试)
    │
    ▼
ParseReadResponse()
    │  解析寄存器0=温度, 寄存器1=湿度
    ├── 触发预警判断 (红/蓝/绿)
    ▼
UpdateData()
    ├── AddHistoryRecord() → DataExportDAL.SaveRecordToSqlite()
    ├── OnDataUpdated → UI 表格更新
    └── OnDataReceived → 停止超时计时器, 重启自动采集
```

### 3.2 异常处理流程（反向）

```
发送超时 (timerTimeOut)
    │
    ├── 重试次数 ≤ 3 → 重发指令
    │
    └── 重试次数 > 3 → SetDeviceOffline()
            │
            ▼
        TryExponentialReconnect()
            │
            ▼
        StartExponentialReconnect()
            │  1s → 2s → 4s → 8s → ... → 30s (指数退避)
            ▼
        reconnectAction() → 关闭 → 重新打开连接
```

### 3.3 数据库操作流程（CRUD）

```
UI 按钮点击 → 输入对话框 → BLL 中转 → DAL 执行 SQL → 刷新 UI 表格
```

---

## 4. 关键设计要点

### 4.1 协议差异透明化

`protocolIndex` 参数贯穿通信层：0 = RTU（串口），1 = TCP。
- **RTU**：PDU + 从站地址 + CRC16
- **TCP**：MBAP 头（7 字节）+ PDU（不含从站地址和 CRC）
- `SendByProtocolAsync()` 和 `ParseModbusResponse()` 内部分支处理差异

### 4.2 事件驱动架构

BLL 不直接引用 UI，通过 8 个事件解耦：
```
ModbusBLL (发布者)                    Form1 (订阅者)
    ──OnLogAdded─────────────────→ AddLog()
    ──OnDataUpdated──────────────→ UpdateModbusTable()
    ──OnDataCleared──────────────→ ClearModbusTable()
    ──OnDataReceived─────────────→ 重置计时器
    ──OnBatchDataReceived────────→ UpdateBatchDataTable()
    ──OnWriteCompleted───────────→ 日志确认
    ──OnModbusException──────────→ 分类处理(业务/通信)
    ──OnReconnectAttempt─────────→ 日志显示进度
```

### 4.3 异常分类决策树

```
收到异常
    ├── 业务异常（Modbus 异常码 0x01-0x08）
    │   └── 跳过重试，记录日志，继续自动采集
    │
    └── 通信异常（超时 / Socket 断连）
        └── 触发指数退避重连
            └── 重连成功 → 恢复采集
            └── 重连失败 → 继续退避，直到最大 30s 间隔
```

### 4.4 三层架构的职责边界

| 层 | 职责 | 不负责 |
|----|------|--------|
| Model | 数据载体，简单的属性 + 构造 | 任何逻辑 |
| DAL | SQLite 增删改查 + 文件导出 | 业务判断、协议解析 |
| BLL | 通信协议、重连、数据更新、异常分类 | 界面渲染、控件操作 |
| UI | 用户交互、控件更新、对话框 | 直接操作数据库或通信 |

### 4.5 内存泄漏防护

- **事件解绑**：`Form1.Dispose()` 中调用 `UnsubscribeModbusBllEvents()`
- **BLL 资源释放**：`Dispose()` 停止 timer、关闭串口/TCP、释放 DAL
- **DAL 资源释放**：`Dispose()` 调用 `SQLiteConnection.ClearAllPools()`
- **析构函数兜底**：`~ModbusBLL()` 和 `~DataExportDAL()` 作为安全网
- **历史记录上限**：`MaxRecordCount` = 1000，防止内存无限增长

### 4.6 配置与代码分离

所有可调参数集中在 [App.config](App.config)：
- 不需要重新编译即可调整：超时时间、重试次数、阈值、定时器间隔
- 数据库路径、IP、端口等环境相关参数集中管理

---

## 5. 文件职责速查表

| 文件 | 行数 | 核心职责 |
|------|------|----------|
| [Program.cs](Program.cs) | 22 | 应用入口，启动 Form1 |
| [Form1.cs](Form1.cs) | 1089 | UI 交互、事件绑定、定时器、批量操作、CRUD 对话框 |
| [Form1.Designer.cs](Form1.Designer.cs) | ~1050 | 控件布局、属性设置、初始化 |
| [ModbusBLL.cs](BLL/ModbusBLL.cs) | 1178 | 串口/TCP 连接、Modbus 报文构建/解析、CRC16、重连、数据更新 |
| [DataExportDAL.cs](DAL/DataExportDAL.cs) | 487 | SQLite 建库建表、CRUD、TXT/CSV 导出 |
| [DataRecord.cs](Model/DataRecord.cs) | 27 | 温湿度数据实体 |
| [App.config](App.config) | 125 | 所有可调参数配置 |

---

## 6. 常见问题与调试入口

### 6.1 通信问题

| 现象 | 排查位置 |
|------|----------|
| 串口打开失败 | `ModbusBLL.OpenSerialPort()` 行 251-268 |
| TCP 连接失败 | `ModbusBLL.ConnectTcpAsync()` 行 291-384 |
| 接收数据 CRC 校验失败 | `ModbusBLL.ValidateCRC()` 行 524-535 |
| 设备无响应（超时） | `Form1.timerTimeOut_Tick()` 行 620-635 |
| 收到异常码 | `ModbusBLL.GetExceptionMessage()` 行 801-814 |

### 6.2 调试技巧

- **查看报文日志**：UI 日志列表显示完整的发送/接收报文（十六进制），可直接比对 Modbus 协议规范
- **修改超时时间**：`App.config` 中 `TcpTimeoutMs` / `TimeOutIntervalMs`
- **调整预警阈值**：`App.config` 中 `HighTempThreshold` / `LowTempThreshold`
- **数据库位置**：`D:\SQLiteData\ModbusRTU_TCP.db`（可在 `App.config` 中修改）

### 6.3 扩展建议

- **新增功能码**：在 `ParseModbusResponse()` 的 switch 中添加 case
- **新增寄存器类型**：在 `GetRegisterName()` 中添加地址映射
- **新增导出格式**：在 `DataExportDAL` 中添加类似 `ExportToXxxAsync()` 方法
- **修改预警规则**：调整 `ParseReadResponse()` 中的阈值判断逻辑

---

*最后更新：2026-08-20*